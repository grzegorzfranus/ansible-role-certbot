---
# Ansible Role: Certbot - Ubuntu 24.04 (Noble) OS-specific constants

# Certbot packages (internal, not user-overridable)
__certbot_os_packages:
  - certbot
  - python3-certbot

# OS-specific prerequisite packages (internal)
__certbot_os_prerequisites:
  - ca-certificates
  - python3
  - software-properties-common
